# Services Module

**DEPRECATED** - This module contains legacy code that depended on removed components.

## Status

- `predictions.py` - **REMOVED** - depended on removed `dao` and `worker` modules

This module was originally for web service business logic but is no longer used in the refactored architecture.
